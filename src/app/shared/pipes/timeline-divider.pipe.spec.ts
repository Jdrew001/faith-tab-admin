import { TimelineDividerPipe } from './timeline-divider.pipe';

describe('TimelineDividerPipe', () => {
  it('create an instance', () => {
    const pipe = new TimelineDividerPipe();
    expect(pipe).toBeTruthy();
  });
});
